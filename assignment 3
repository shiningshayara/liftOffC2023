#include<stdio.d>
int main() 
{
int n, sum=0;
printf("enter a value whose sum of digits are to be calculated");
scanf("%d",&n);
do
{
   sum=sum+(n%10);
   n=n/10;
   }
   while(n>0);
  printf("sum of digits is=%d",sum);
  return 0;
  }